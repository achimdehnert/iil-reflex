"""
REFLEX Review Plugins — Auto-discovered by ReviewEngine (ADR-165 §5.1).

Each plugin module must expose a `plugin` attribute implementing ReviewPlugin.
New plugin = 1 Python file in this directory, no server change needed.
"""
