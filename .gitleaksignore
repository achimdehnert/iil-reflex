# IIL Platform — gitleaks fingerprint suppressions (siehe platform docs/templates/.gitleaks.toml).
# Format: <commit>:<path>:<rule>:<line>
#
# Aktuell keine Suppressions.
#
# Historie: hier stand ein Fingerprint fuer das API-Key-Fixture in
# tests/test_review_plugins.py. Commit-gepinnte Fingerprints ueberleben weder
# einen Rebase noch eine Zeilenverschiebung durch fremde PRs — genau das ist
# passiert: der Eintrag zeigte auf einen nie gemergten Branch-Commit und auf
# Zeile 166, waehrend das Fixture inzwischen weiter unten steht. Zusaetzlich
# verbietet der Triage-Hint des Artefakt-Scans eine Intent-Suppression fuer
# alles, was im sdist publiziert wird. Das Fixture setzt seinen Wert daher zur
# Laufzeit zusammen — die Quelle enthaelt kein Secret-foermiges Literal mehr
# und braucht keine Suppression.
